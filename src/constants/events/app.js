
// Occurs when the application first loads brining in config data
export const APP_LOADED = Symbol('APP_LOADED');

// Occurs when an alert message has been closed
export const ALERT_DISMISSED = Symbol('ALERT_DISMISSED');

// Occurs whenever a new quote path is initiated
export const NEW_QUOTE_STARTED = Symbol('NEW_QUOTE_STARTED');

// Occurs when a selection is made on the opportunity selection page
export const OPPORTUNITY_SELECTED = Symbol('OPPORTUNITY_SELECTED');

export const CUSTOMER_INFO_RESET_SELECTED_LOCATIONS = Symbol('CUSTOMER_INFO_RESET_SELECTED_LOCATIONS');
